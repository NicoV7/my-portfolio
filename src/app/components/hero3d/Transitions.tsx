'use client'

/**
 * White-canvas direction: the dark seam tunnels are retired (they read as dark
 * intrusions on the white world). The Persona-5 DOM seam beat (DriveTransition)
 * carries the region transition instead.
 */
export default function Transitions() {
  return null
}
