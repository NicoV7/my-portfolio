'use client'

/**
 * White-canvas direction: the neon roadside sign gantries are retired (they only
 * made sense in the night-city world). Stop labels live in the DOM
 * `MilestoneOverlay` cards instead, keeping the 3D scene to just path + car.
 */
// parked stub: keeps the prop signature for call sites while rendering nothing
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function Destinations(_props: { activeIndex: number }) {
  return null
}
