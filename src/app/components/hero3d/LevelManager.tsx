'use client'

/**
 * White-canvas direction: the themed city scenery (Berkeley/Tokyo/SF/…) is
 * retired. The path + the car on the near-white ground carry the scene, so this
 * mounts nothing. The region files stay in-tree, just unmounted.
 */
// parked stub: keeps the prop signature for call sites while rendering nothing
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function LevelManager(_props: { activeIndex: number }) {
  return null
}
